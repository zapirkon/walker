{
  "name": "walker",
  "version": "1.0.0",
  "description": "The alphabet walker",
  "author": "walker",
  "private": "true",
  "main": "index.ts",
  "scripts": {
    "prestart": "tsc",
    "start": "node dist/index.js",
    "watch": "ts-node-dev --respawn ./src/index.ts",
    "test": "jest",
    "test-single": "jest --watchAll --silent=false -t",
    "test-watch": "jest --watchAll",
    "test-unit": "jest --watchAll --testPathIgnorePatterns automation",
    "test-auto": "jest automation"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/jest": "^28.1.6",
    "jest": "^28.1.3",
    "ts-jest": "^28.0.6",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.7.4",
    "weak-napi": "^2.0.2"
  }
}
